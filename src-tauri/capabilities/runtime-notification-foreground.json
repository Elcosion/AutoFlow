{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "runtime-notification-foreground",
  "description": "Allows the main window to foreground an explicit runtime notification.",
  "windows": ["main"],
  "permissions": ["core:window:allow-unminimize", "core:window:allow-set-focus"]
}
